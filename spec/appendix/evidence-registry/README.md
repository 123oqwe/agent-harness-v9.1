# Evidence Registry

This directory tracks all evidence packages generated during implementation.

Evidence packages are stored in `artifacts/phase-N/` during implementation.
Each evidence package must be generated from actual command output.
