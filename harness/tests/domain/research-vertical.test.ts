import { describe, it, expect } from 'vitest';

describe.skip('vertical domain test — requires Phase 2 composition root', () => {
  it('placeholder until harness.ts is available in this branch', () => {
    expect(true).toBe(true);
  });
});
